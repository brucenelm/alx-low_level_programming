#include "main.h"

/**
 * _isupper - check if character is uppercase
 * @a: character to check
 *
 * Return: 1 if c is uppercase,
 * 0 otherwise.
 */
int _isupper(int a)
{
	return (a >= 'A' && a <= 'Z');
}
